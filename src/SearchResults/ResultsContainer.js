import React, { Component } from 'react';

export default class ResultsContainer extends Component {
  // loop through search results
  // insert ResultsItem(s) into Container
  render() {
    return <section>{/* display results items here */}</section>;
  }
}
